<?xml version="1.0"?>
<tool id="chakin_phylogeny_add_cvterms" name="phylogeny" version="@WRAPPER_VERSION@.0">
 <description>add_cvterms</description>
	<macros>
		<import>macros.xml</import>
	</macros>
	<expand macro="requirements"/>
	<command detect_errors="aggressive">
chakin phylogeny add_cvterms


| jq -S . > $results
</command>
	<inputs>
    <!-- arguments -->

    <!-- options -->

	</inputs>
	<outputs>
		<data format="json" name="results"/>
	</outputs>
	<help>
Make sure required cvterms are loaded

@HELP@
	</help>
</tool>
